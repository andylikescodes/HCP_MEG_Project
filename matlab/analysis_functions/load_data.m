load 'left_channels.mat';
load 'right_channels.mat';
load 'middle_channels.mat';
load '100307_MEG_3-Restin_rmegpreproc.mat';

%channel_arrangments = [left_channels, right_channels, middle_channels];

%resorted_data = resort_data_by_channels(data, channel_arrangements);
